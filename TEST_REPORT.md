# تقرير التحقق — منصة خطوات MVP 1.2

تاريخ التحقق: 24 أغسطس 2026

## ما تم التحقق منه

- فحص الصياغة لجميع ملفات PHP: ناجح (59 ملف PHP، بدون أخطاء Syntax).
- بناء ملفات الواجهة عبر `node scripts/build.mjs`: ناجح.
- سلامة قاعدة SQLite عبر `PRAGMA integrity_check`: النتيجة `ok`.
- البيانات التجريبية الأساسية موجودة للخدمات والخطوات والأسئلة والقواعد والمقالات والحاسبات والتحديثات.
- تم تثبيت مدير الحزم المطلوب في تعريف المشروع بصيغة `packageManager: pnpm@11.22.0`، مع ملف قفل `pnpm-lock.yaml`.

## قيود بيئة الاختبار الحالية

بيئة التنفيذ المستخدمة لإنشاء الحزمة تحتوي PHP 8.4.23، لكنها لا تحتوي امتدادي `pdo_sqlite` و`mbstring`، لذلك لا يمكن إجراء اختبار تكامل كامل للصفحات المعتمدة على قاعدة البيانات داخل هذه البيئة نفسها. كما أن `curl` غير متوفر، وهو مطلوب فقط لموصلات خارجية مثل بعض تكاملات الذكاء الاصطناعي.

تم التحقق من أن التطبيق يعرض رسالة إعداد واضحة بدل انهيار غير مفهوم عند غياب `pdo_sqlite`.

## متطلبات الخادم قبل التشغيل

- PHP 8.2 أو أحدث.
- `PDO` و`pdo_sqlite` و`mbstring` و`openssl`.
- `curl` عند تفعيل موصلات HTTP الخارجية/الذكاء الاصطناعي.
- صلاحية كتابة للمجلدات داخل `storage/`.
- جعل `public/` هو Document Root.
- pnpm 11.22.0 مطلوب للبناء/التطوير فقط؛ الملفات المبنية جاهزة داخل `public/assets/`.

راجع `README_AR.md` لتعليمات التثبيت والتشغيل والـ Cron وبيانات الحسابات التجريبية.
